create table pets (
      name          CHAR NOT NULL,
      species       CHAR NOT NULL,
      breed         CHAR NOT NULL,
      age           CHAR NOT NULL,
      temperament   CHAR NOT NULL,
      gender        CHAR NOT NULL);